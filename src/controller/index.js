const location = require('./location')
const profile = require('./profile')
const neighbourhood = require('./neighbourhood')
const city = require('./city')

module.exports = {location, profile, neighbourhood, city}
